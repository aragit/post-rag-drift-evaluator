apiVersion: 1

datasources:
  - name: Sentrix-Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: GET
      timeInterval: 5s
      queryType: range
    secureJsonData: {}
