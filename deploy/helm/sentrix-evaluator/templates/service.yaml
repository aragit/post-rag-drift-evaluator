apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentrix-evaluator.fullname" . }}
  labels:
    {{- include "sentrix-evaluator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentrix-evaluator.selectorLabels" . | nindent 4 }}
